# ===============================
# = JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.open-in-view=false
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.generate_statistics=true
#spring.jpa.properties.hibernate.format_sql=true
# ===============================
# = DATA SOURCE
# ===============================
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://${host}:${port}/${database}
spring.datasource.username=${username}
spring.datasource.password=${password}
# ===============================
## = AWS S3
## ===============================
#storage.key=$AWS_ACCESS_KEY_ID
#storage.secret=$AWS_SECRET_ACCESS_KEY
#storage.endpoint=$AWS_ENDPOINT
#storage.region=$AWS_REGION
#storage.bucket=$AWS_BUCKET
#cloud.aws.stack.auto=false
# ===============================
## = DO Spaces
## ===============================
storage.key=${DO_SPACES_KEY}
storage.secret=${DO_SPACES_SECRET}
storage.endpoint=${DO_SPACES_ENDPOINT}
storage.region=${DO_SPACES_REGION}
storage.bucket=${DO_SPACES_BUCKET}
# ===============================
# = SMTP EMAIL
# ===============================
spring.mail.host=$SMTP_HOST
spring.mail.username=$SMTP_USERNAME
spring.mail.password=$SMTP_PASSWORD
spring.mail.port=587
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# ===============================
# = LOG
# ===============================
#logging.level.org.hibernate.type=trace
#logging.level.org.hibernate.stat=debug
#logging.level.org.springframework.web=debug
# ===============================
